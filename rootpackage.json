{
  "name": "the-tales-quiz",
  "version": "1.0.0",
  "description": "The Tales - Ultimate Quiz Game",
  "scripts": {
    "start": "cd backend && npm install && node server.js",
    "build": "cd backend && npm install",
    "postinstall": "cd backend && npm install"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}
